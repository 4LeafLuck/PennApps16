import React from 'react';
import { StyleSheet, Text, View, Image, Asset, Button, ImagePicker, TouchableOpacity } from 'react-native';

export default class App extends React.Component {
  render() {
    return (
      <View style={styles.container}>
        <Text style={styles.title}>Step Style</Text>
	
	<Image source={require('./assets/images/Profile_avatar_placeholder_large.png')} />
	
      </View>

    );
  }
}

const styles = StyleSheet.create({
  title:   {
    flex: 1,
    lineHeight: 120,
    fontSize: 60,
},
  container: {
    flex: 1,
    backgroundColor: '#dff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
